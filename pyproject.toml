[build-system]
requires = ["flit_core >=3.2,<4"]
build-backend = "flit_core.buildapi"

[project]
name = "muTopOpt"
authors = [
    { name = "Martin Ladecky", email = "martin.ladecky@imtek.uni-freiburg.de" },
    { name = "Lars Pastewka", email = "lars.pastewka@imtek.uni-freiburg.de" },
]
description = "FFT-accelerated FE topology optimization of mechanical metamaterials, built on muGrid"
readme = "README.md"
license = { file = "LICENSE" }
requires-python = ">=3.10"
dynamic = ["version"]
dependencies = [
    "numpy",
    # 1.4.0 is the first release whose float32 reductions return double
    # instead of narrowing to inf, and the first to export
    # muGrid.Solvers.FLOAT32_RTOL_FLOOR, which muTopOpt.precision reuses.
    "muGrid>=1.4.0",
    "NuMPI",
]

[project.optional-dependencies]
test = ["pytest"]
scipy = ["scipy"]  # for the alternative serial optimize_lbfgs
mpi = ["mpi4py"]

[tool.flit.module]
name = "muTopOpt"
